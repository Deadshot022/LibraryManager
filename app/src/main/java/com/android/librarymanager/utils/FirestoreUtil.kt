package com.android.librarymanager.utils




object FirestoreUtil {
//    private val db = FirebaseFirestore.getInstance()
//
//    fun addUser(user: Map<String, Any>) {
//        db.collection("users").document(user["uid"] as String).set(user, SetOptions.merge())
//    }
//
//    // Add more functions as needed, e.g., getUser, updateUser, deleteUser, etc.
}